# 智慧物流系統開發進度日誌

## [2026-06-01] 結案回傳補強與成功頁資料同步修正

### 1. `submitFinalDelivery()` 回傳結構補強 (`Code.js`)
- **完整資料回傳**：將結案函式由單純 `success` 狀態，補強為可直接供前端使用的完整資料物件，新增回傳欄位包含：
  - `status`
  - `signTypeDesc`
  - `signPhotoUrl`
  - `damagePhotoUrl`
  - `batchTasks`
- **舊前端相容**：保留原本已在前端使用的欄位，例如 `branch`、`weight`、`arrivalTime`、`sameAddrCount`、`remainingCount`、`remainingTasks`，避免既有成功頁與下一站導引流程中斷。
- **批次結案同步**：同地址批次結案時，回傳資料也一併保留 `batchTasks`，讓前端可持續處理同地址合併單與後續畫面更新。

### 2. 成功頁資訊顯示修復 (`Index.html`)
- **狀態顯示補齊**：成功頁原本讀取 `res.signTypeDesc || res.status || ""`，但後端未回傳對應欄位，導致狀態文字可能空白；本次補強後可正確顯示 `已送達` 或 `配送失敗`。
- **後續導航資訊維持**：同地址待處理筆數、下一站任務清單與到貨時間等欄位持續可用，維持原本的成功頁導引體驗。

### 3. 系統佈署
- 使用 `clasp push` 將更新同步至 Apps Script 生產環境，確保雲端版本與本機內容一致。

## [2026-05-28 V37.23] 阿榮派車卡片重塑、明細一行收折與 OCR 容錯升級

### 1. 阿榮派車 UI 重塑與分公司後綴顏色代替 (`DispatchSystem.html`)
- **分公司專屬顏色單號**：移除單號的原有中文後綴（如 `-高`、`-安` 等），改以分公司的專屬設計顏色（如高雅瓷粉色、安帝嘉綠色）做成高質感的背景標籤，並將單號**直接移至地址的正後方**。
- **搜尋與篩選完整相容**：此修改僅改變前端顯示，底層的原始 `branch` 與後綴特徵仍完美保留，確保頭部與側邊欄的分公司篩選、文字搜尋等核心大腦功能完全不受影響。
- **靜態欄位移除與收折明細**：徹底移除了原本佔用空間的靜態「尺寸」與「箱數」，替換為高質感的 `📦 明細 (X) ▼` 收折按鈕。點選時可動態展開或收合該訂單內的商品編號、尺寸與數量。
- **卡片第三行備註加入**：在卡片第三行新增了獨立的備註文字行，能以顯眼的黃色直接呈現該訂單之備註，方便排車人員直覺對照。

### 2. 倉庫驗貨與司機排序彈窗「明細一行化」緊湊重合 (`DispatchSystem.html` / `Warehouse.html`)
- **單行緊湊呈現**：為了極致利用手機及網頁寬度，將**派遣工作站**、**司機排序彈出視窗**，以及**倉庫驗貨系統**展開後的明細行排版全面「一行化」重塑：
  - 格式：`[編號]    [尺寸]    [數量]片`
  - 使用半透明色系做視覺隔離，並改用 dashed 虛線做間隔，視覺上極致精緻且毫不凌亂。

### 3. LINE 派車單分享圖片排版同步 (`DispatchSystem.html`)
- **圖片單號標籤化**：同步更新 `generateListImage` 分享圖片繪製範本，在地址後方繪製該分公司專屬顏色的單號標籤。
- **平鋪明細與備註**：直接將該單據內的所有商品編號、分項尺寸、數量以及備註，以精美的深色區塊平鋪展示在圖片卡片下方，確保司機能在 LINE 靜態圖片中一眼看清所有細節。

### 4. Google Vision OCR 雙字母帶空格 (如 `E E`) 識別容錯與多品項「代號定位切分」優化 (`OcrEngine.js`)
- **`E E` 帶空格識別修復**：為解決 Google Vision OCR 將高雅瓷銷貨單底部的 `EE` 辨識為含有空格的 `E E`，導致無法匹配 `\b(AA|EE|SS|WW)\b` 進而 Fallback 誤判為「漢樺」的問題。將正則表達式更新為 `\b(A\s*A|E\s*E|S\s*S|W\s*W)\b`，在匹配成功後自動剔除空格並標準化為大寫。
- **代碼定位式切分算法**：廢除原本極易因 Google Vision OCR 直行排版而失效的「項次分割」算法，升級為全新的「產品代碼定位分割法」。先提取出所有真正的商品代號，並以這些代號作為邊界進行精確段落切割，徹底解決了因表格分欄導致的第二品項漏失 Bug。

### 5. 生產環境 Web App 熱部署
- 全數檔案完成本機同步並透過 CLASP 推送至 Apps Script 生產環境，所有新版功能均已即時生效。

## [2026-05-26 V37.22] 經銷商查詢配送進度歷史日期漂移徹底修復與 Clasp 正式版本發布

### 1. 後端經銷商 API 回傳日期欄位補齊 (`11_DealerAPI.js`)
- **破案根本原因**：排查發現經銷商專用 API 端點 `handleDealerApiRequest_V11` 在重組並回傳 `tasks` 清單給前端時，其物件結構完全漏掉了 `date` 欄位（`date: t.date` 未寫入）。
- **完全補全屬性**：在 `tasks` 的 map 函數中補上 `date: t.date`。這解決了前端 `t.date` 永遠為 `undefined`，進而被迫執行防禦性今天日期 `new Date().toLocaleDateString('zh-TW')` 兜底，導致 timeline 上歷史訂單（如 5/25）無條件全部顯示為今日（5/26）的致命 Bug。

### 2. OCR 民國年份、訂單號逆向推算與前端 local 時區解析鎖定 (`OcrEngine.js` / `Code.js` / `tracking.html`)
- **ROC 民國年相容**：在後端 `normalizeDate_Core` 加上民國年與西元年轉換防護（年份小於 200 時自動加上 1911），使其正確識別 `115` 等民國年為 `2026`。
- **單號前 6 碼逆向日期推算**：在 `OcrEngine.js` 實作防禦機制，當 OCR 完全遺漏日期時，從 9 碼訂單號前 6 碼 (YYMMDD) 提取並轉換為民國年防錯帶入，徹底阻斷今日日期兜底。
- **前端時區防漂移鎖定**：在前端 `tracking.html` 將 date 的替換符號由「斜線轉連字號」修正為「連字號轉斜線 (YYYY/MM/DD)」，強制瀏覽器在本地時區（GMT+8）解析，阻斷 ES6 UTC 時區差（+8小時偏移）造成的隔日顯示 Bug。

### 3. 生產環境第 371 版 (Version 371) 熱重載部署
- 全數檔案本機同步並透過 CLASP 推送，且執行 **`clasp deploy -i ...`** 成功升級發布為最新之 **第 371 版 (Version 371)**，正式版即時熱重載生效，徹底杜絕了 clasp push 後 Web App 未熱部署的版本滯後問題。

## [2026-05-25 V37.21] 歷史加油無紀錄隱藏與保養上期綠色高亮

### 1. 歷史加油無紀錄日期過濾
- **無加油記錄日期完全隱藏**：修改加油歷史查詢邏輯，當司機點擊 `🔍 歷史加油查詢與修正` 時，前端將自動過濾並移除加油金額與公升數皆為空白或為 0 的無效出勤日期，只呈現真正有加油的日期紀錄，大幅精簡版面。

### 2. 前次車輛保養日期綠色高亮
- **極光綠視覺高亮**：在「車輛管理」保養項目中，若系統偵測到上期保養歷史日期（例如：上次: 6/15），會以極致亮麗的 **Premium Neon Green (極光綠: `#4ade80`)** 進行字體加粗與著色顯示，與無紀錄的灰色形成強烈視覺對比，醒目引導司機掌握上次保養排程。

### 3. 生產環境第 363 版 (Version 363) 熱重載部署
- 全數檔案本機同步並透過 CLASP 推送，發布為最新之 **第 363 版 (Version 363)**，正式版即時熱重載生效。

## [2026-05-25 V37.20] 司機歷史加油紀錄查詢與修正功能上線

### 1. 歷史加油紀錄查詢與修正
- **加油分頁專屬入口**：在「車輛管理」的「加油登記」分頁中，全新增設 `🔍 歷史加油查詢與修正` 按鈕。
- **加油明細卡片還原**：點選後，系統會透過升級後的 `getMileageHistory_V11` 接口，智慧逆向擷取指定車牌的最後 15 次出勤加油明細（包含加油金額、加油公升數）。
- **即時數值修改與快取清除**：點選金額或公升數旁的 `✏️` 圖示，司機可直接於彈窗修正數值。修正數值將回傳寫入 `每日行程表`（`SHEET_SCHEDULE`）對應列之 `加油金額` 或 `加油公升數` 欄位，並即時清除快取，保證資料的準確性。

### 2. 生產環境第 362 版 (Version 362) 熱重載部署
- 全數檔案本機同步並透過 CLASP 推送，發布為最新之 **第 362 版 (Version 362)**，正式版即時熱重載生效。

## [2026-05-25 V37.19] 司機里程修正歷史紀錄查詢與保養上次紀錄智慧帶入

### 1. 司機里程修正與歷史紀錄查詢
- **歷史出勤軌跡查詢**：在「發車/收工登記」主面板底部，全新增設 `🔍 歷史里程查詢與修正` 入口，動態逆向查詢該車牌最近 15 次出勤登錄（包含日期、上班里程、下班里程及單日總行駛公里數）。
- **即時修正與總里程重算**：點擊上班/下班里程旁的 `✏️` 圖示，司機即可在彈窗中直接進行里程數據修正。系統回傳至後端寫入 `每日行程表`（`SHEET_SCHEDULE`）的指定列號，並在背景自動完成 **「單日總里程」** 的動態更新（結束里程 - 起始里程）。
- **極致預防呆與手動調整**：不論是出發前或收工時的自動里程帶入，司機皆可直接點擊輸入框進行靈活手動修改，享有極高自主權。

### 2. 車輛保養上期紀錄智慧帶入與預勾選
- **上期保養資料自動讀取**：重構「車輛保養」回報模組，調用全新的 `getVehicleMaintContext_V11` 接口，從 `車輛保養`（`SHEET_MAINT`）工作表逆向搜尋該車牌最後一次登錄的保養明細。
- **欄位預填與前回項目預勾選**：
  - 本次里程：自動填入上次保養里程做為參考；
  - 下回里程：自動帶入前回預定的下回保養里程；
  - 備註：預填前回備註；
  - 保養項目：智慧還原並「預先勾選」上次保養過的項目（如變速箱油、冷氣濾網等），極大化減少司機重複輸入之負擔。
- **上次間隔天數運算**：自動向後端拋送上次保養日期 `lastDate`，由後端智慧計算距今日天數並填入對位。

### 3. CLASP 生產環境第 361 版 (Version 361) 熱重載部署
- 全數檔案完成本機同步並透過 CLASP 推送，發布為最新之 **第 361 版 (Version 361)**，正式版即時熱重載生效。

## [2026-05-25] 司機端 App 效能極速優化、iOS 永久會話保險庫與破損相片直傳結案

### 1. iOS 永久憑證保險庫與三層登入保障 (`IDBVault`)
- **突破 ITP 沙盒封鎖**：針對 iOS 17+ Safari/WKWebView (LINE 內嵌瀏覽器) 會頻繁自動清理 `localStorage` 和 Cookie 導致司機重覆登出的問題，特別在前端引入了底層資料庫級別的 **IndexedDB 保險庫 (`IDBVault`)**。
- **三軌並行恢復機制**：每當司機進行登入、URL 參數載入或車牌變更時，系統會同步寫入 `localStorage`、`Cookie` 與 `IDBVault`。當偵測到 `localStorage` 遭 iOS 抹除時，會在一瞬間異步讀取 IndexedDB 憑證並重構會話，達到 iOS 司機與 Android 一致的永久免登入順暢體驗。
- **安全登出清理**：重構「切換人員」按鈕，調用 `App.logoutAndSwitch()` 同步徹底清理 `localStorage`、`Cookie` 及 `IDBVault` 中的所有憑證痕跡，保障資訊安全性。

### 2. 跨假日/跨天歷史里程智慧繼承
- **動態表頭欄位檢推**：將 `Code.js` 的 `getYesterdayMileage_V11p9` 升級為全動態欄位檢索。系統自動對 `每日行程表`（`SHEET_SCHEDULE`）的表頭進行掃描定位「車牌號碼」、「結束里程」與「起始里程」欄位，防範欄位位移出錯。
- **逆向時間指針檢索**：改由最底端最新資料向舊資料逆向掃描該指定車牌的歷史紀錄：
  - **首選**：取該車輛最近一次下班登錄的 **「結束里程」** 作為今日的出發起點，完美解決跨越週末、國定假日或非連續出勤日的里程跳回原點問題。
  - **安全回退**：若因司機前一日未收工而無結束里程，會安全回退參考當日「起始里程」，極致避免程式出錯。

### 3. 行內輕量讀取 spinner 與載入效能極速化
- **極簡轉圈取代重度加載**：徹底精簡原 Cyberpunk `smartLoader` 卡頓特效。在 CSS 中定義極輕量的 **Premium Minimal Neon Spinner（霓虹藍旋轉環）**。
- **完全關閉耗電特效**：強行利用 CSS 阻斷 `.pulse-ring`, `.grid-bg`, `.sl-scanlines`, `#slParticles`, `.sl-sysbar` 等重度渲染元素，並在 JS 中停用對應的粒子與時鐘 interval，感知加載淡出時間由 `0.6s` 壓縮至 `0.15s`。
- **無阻礙行內讀取動畫**：針對「今日/未來任務」切換（Tab 切換）以及「↻ 重新整理」按鈕，改採在列表容器 (`#taskList`) 內顯示**非阻塞的行內讀取動畫**，徹底拔除彈出全螢幕遮罩的突兀感。

### 4. 按鈕物理與觸覺回饋 (:active)
- **觸覺反應強化**：為選人畫面的司機姓名按鈕、發車登記、結案及關閉按鈕，引入現代物理彈性點擊效果（`:active { transform: scale(0.95); filter: brightness(1.2); }`），按下一瞬間即有生動的收縮回饋，提升操作回饋感。

### 5. 貨物破損相片直傳結案與 Canvas 紅色警示渲染
- **放寬結案限制**：在 `submitFinal()` 中，若司機選擇「貨物破損：是」，系統防呆放寬為「破損照片」或「簽名照片」二擇一即可提交，無須強行綁定簽名。
- **Canvas 紅色警示結案回執**：當貨件破損時，Canvas 生成之結案單回執自動切換為 **紅色高亮主題色 (`#ef4444`)**、標題自動標記 `【貨物破損回報】`，底部已送達印章自動轉為亮紅色的 **「有破損 / DAMAGED」**，以破損照片為主圖。
- **後端空值防崩潰**：在後端 `submitFinalDelivery` 追加空值安全處理，即使無簽收圖 contents 也能安全執行，保證交易 100% 成功。

### 6. CLASP 生產環境自動化部署
- 透過 clasp 將所有優化之 `Index.html` 及 `Code.js` 等 18 個檔案推送，成功建置發佈為最新之 **第 360 版 (Version 360)** 正式部署，免去手動發佈步驟。

## [2026-05-22] 經銷商權限漏洞徹底封閉與前後端安全熔斷防禦

### 1. 後端安全隔離歸一化 (11_DealerAPI.js)
- **回傳字串熔斷**：為了解決當 Spreadsheet 白名單中的角色欄位因為大小寫不同（如 `FREE`、`Free`）、前後夾帶空白字串，或是因標題未對正導致 `userGrade` 解析為空值 `""`，進而引發前端比對邏輯失效的問題。
- **後端安全強制歸一**：在 API 的最終 JSON 封裝端進行防護強化——只要後端邏輯判定該帳號為 `isFreeGrade = true`（受限等級），回傳給前端的 `grade` 屬性一律**強制熔斷輸出為標準小寫 `"free"`**，不再信任試算表原始輸入。

### 2. 前端預設受限熔斷防禦 (tracking.html)
- **排除型角色比對**：全面修改 `tracking.html` 中三處定義 `isUserFreeGrade` 的核心邏輯。
- **默認最嚴格權限**：將判斷機制由「主動比對 free」升級為與後端對齊的「排除型熔斷」——**「除非明確被指派為高級管理與業務權限（如 `king`, `sales`, `admin`, `queen`, `premium`），否則一律默認最嚴格的 `free` 等級」**！確保在任何異常或空值情況下，前端都不會發生溢出解鎖。

### 3. 修復 100% 匹配 Bug (isOurTask 嚴重筆誤)
- **破案與修復**：排除客戶「德思特尼」登入後能看到同一台車上其他客戶（金磚、國泰）訂單明細且標註為專屬單的嚴重 Bug。
- **筆誤漏洞起源**：查出 `isOurTask` 中原本寫有嚴重手誤的判斷條件：
  - `if (cleanUser && (cleanUser.includes(cleanTaskCust) || cleanUser.includes(cleanDealer))) return true;`
  - 因為登入者姓名如 `"德思特尼-雨潔"` (`cleanUser`) 永遠包含所屬公司 `"德思特尼"` (`cleanDealer`)，導致後半句在比對其他無關站點時，永遠靜態回傳 `true`，強行解鎖了整台車的其他明細！
- **精準修正**：將該行代碼後半段修正為對稱的任務對象比對：
  - `if (cleanUser && (cleanUser.includes(cleanTaskCust) || cleanTaskCust.includes(cleanUser))) return true;`
  - **成效**：徹底鎖死非自己專屬之配送點，完美呈現安全的「灰色 + 去敏感路段」防護。

### 4. 系統安全部署
- **GAS 生產環境熱部署**：使用 clasp 工具將全部修正成果發布並部署至最新的 **第 355 版**。
- **cPanel 前端更新規範**：指引系統擁有者同步上傳覆蓋 cPanel 中的 `/public_html/ai_line/tracking.html` 網頁檔案，完成前後端同步的安全閉環。

## [2026-05-21] 配送 ETA 精準化、路途行駛可視化與 UI/UX 深度改造

### 1. 智慧型累積 ETA 精準估算引擎
- **首站即時導航**：當司機發車啟動導航時，首站直接讀取 Google Maps 提供的秒級精準時間 `vPos.targetPreciseMin`。
- **後續站點滾動累積 (只估下二站)**：
  - 實作前後站經緯度 Haversine 距離公式並加入 1.3 道路距離加權係數。
  - **30km/h 車速車程計算**：完美實作 `Leg車程(分鐘) = 距離(公里) * 2` 的簡約高效換算。
  - **累加下貨時間**：隨後每一站抵達時間自動累加前一站的 15 分鐘下貨時間與該路段車程。
  - **深度簡化顯示（只估下二站）**：為避免過遠的預估失去準確性與意義，**系統智慧限制僅估計與顯示「下二站」（即當前配送站與下一個即將抵達站）的預估時間**（下下站以 `10:15` 等真實時鐘格式顯示在尊榮淡紫色漸層標籤中）。其餘更遠的站點則維持乾淨的日期與星期顯示，使介面重點聚焦且完美實用！

### 2. 卡車路途中位物理懸掛可視化
- **行駛行進中模擬**：為滿足「司機在路途中，卡車應顯示在兩站中間」的要求，當站點狀態為配送中（`isCurrent`）時，系統會判定卡車正處於行駛中狀態。
- **物理中位懸掛**：動態計算卡車 top 偏移量（首站 `i=0` 設為 `-36px`，其餘站設為 `-48px`），讓炫彩脈衝卡車圖示精準懸掛於兩站垂直捷運線段的正中央，呈現完美的行進中（In Transit）動態視覺效果。

### 3. 日期與狀態顯示之極致簡化
- **去贅字客戶名**：移除了卡片標題上所有的客戶公司名稱赘字，只保留核心商業個體。
- **狀態標籤精準分級**：
  - **送達/完成**：顯示 `M/D 周幾 送達`
  - **當前配送中**：只有當前司機正在前往的 active 站點，且在已發車狀態下，才顯示 `配送中`。
  - **後續未到達站點**：徹底去除 `配送中` 贅字，僅優雅顯示 `M/D 周幾`，免除經銷商與客戶對後續站點的狀態誤解。
- **去等候文字**：徹底移成了 Timeline 詳情頁中冗餘的 "等候司機啟動導航" 灰色 Badge，保持版面極簡與高級感。

### 4. 退貨與收貨紅色高亮標籤 (收退貨)
- **多端紅色警示**：系統自動偵測站點合併單中任一筆包含「退貨」或「收貨」等任務類型。
- **視覺強化**：
  - FREE 等級用戶：於去敏感路名後方增加高醒目紅色 `(收退貨)` 字樣。
  - 本公司專屬單/非 FREE 用戶：於客戶名稱後方同步高亮加註紅色 `(收退貨)` 警示標籤，方便人員直覺辨識異常收退件。

### 5. 簽收海報與銷貨單 UI/UX 改版與全螢幕放大
- **大畫幅直式簽收卡片**：將 `receiptModal` 中的照片顯示區高度自 `200px` 大幅加高至 `350px`，並將排版改為 `object-fit: contain` 與曜石黑背景，完美呈現司機直拍的簽收單據照片，徹底解決裁剪與遮擋痛點。
- **點擊放大與手勢支援**：
  - 點擊簽收海報的照片即可直接彈出全螢幕大圖預覽。
  - 新增「點此在新分頁放大原圖 (支援縮放)」炫彩快捷鍵，利用瀏覽器原生高性能手勢縮放、旋轉與列印，完美解決銷貨單與簽收單大圖查閱與放大需求。

### 6. 安全隔離 (Dealer API) 座標解鎖
- **車輛位置解鎖**：修改 `11_DealerAPI.js` 中的 mapping 邏輯，同步解鎖並輸出車輛的即時經緯度座標 `lat` 與 `lng`，供前端高精度路網進行 ETA 測算，在保障商業數據隔離安全的同時，完美支撐大腦定位運算。

## [2026-05-20] 派送進度查詢 portal 權限隔離、動態定位與 ETA 智慧優化

### 1. 經銷商白名單與測試環境覆寫
- **白名單權限釐清**：確認 `系統白名單`（公司內部業務/司機/管理者）與 `經銷商白名單`（外部經銷商客戶查詢）之分工，並協助用戶排除等級變更後未生效的疑問。
- **免改單測試覆寫 (URL Overrides)**：
  - 在 `tracking.html` 中實作開發者測試參數：新增 `?grade=free`（強制切換為 FREE 等級）與 `&dealer=安帝嘉`（強制模擬特定經銷商為「我的公司」）。
  - **成效**：開發與調度人員無需手動修改 Google Sheets，即可瞬間在瀏覽器中完整切換並預覽兩大權限的真實顯示效果。

### 2. FREE 等級與非 FREE 等級之極致權限隔離與高亮顯示
- **FREE 等級 (其他公司點)**：
  - 路段名稱自動脫敏（僅截取路段名如 `中華路`，不顯示幾號、樓層與客戶名稱）。
  - 全數採用**統一的高雅翠綠色 (`#10B981`)** 配色與簡約圓角標籤，且設定為收合、不可點擊點開，完美保護同業商業隱私。
- **FREE 等級 (我司專屬點)**：
  - 解鎖顯示完整客戶名稱、分公司專屬彩色配色與詳細地址，且可自由點擊展開查看簽收照片與銷貨單。
  - **高亮專屬標籤**：新增紫靛色漸層漸變的 **` 您的專屬單`** 3D 呼吸燈標籤，幫助客戶在時間軸上 1 秒認出自己的貨件。
- **非 FREE 等級 (KING/Sales/Admin)**：
  - 完整解鎖所有站點，直接在站點標題以分公司專屬色彩融合顯示客戶與路段名稱（如 `金磚 (高職東街)`）。

### 3. 倉庫出發站「中立化」設計
- **站數統計邏輯修正**：修改 `tracking.html` 資料迴圈，頂部的「此路線總站」、「已送達」與「剩餘站」統計**徹底排除「倉庫發車」**，僅計算實際需要配送的站點。
- **UI 中立渲染**：移除倉庫發車的 checkmark 或已送達綠色標章，改為中立的灰色倉庫圖示與灰色邊框，防止一發車即判定有已送達站點的誤解。

### 4. 司機導航 ETA 延遲智慧計算
- **導航狀態連動**：在 Timeline 上，ETA（預計抵達時間）計算改與司機的即時導航狀態綁定。
- **視覺呈現**：
  - 若司機在 App 端尚未按下「導航至此站」，Timeline 詳情頁中會以灰色調精緻呈現：**` 等待司機啟動導航`** 狀態。
  - 司機按下導航後，系統才會即時啟動 GPS 估算並動態渲染精準的：**` 預計 X 分鐘後抵達`**，徹底杜絕虛假 ETA。

### 5. 炫彩閃爍定位卡車（二合一與精準物理定位）
- **消滅重複車輛**：移成了原本在垂直軌道上算百分比的第二台卡車，維持 Timeline 畫面上僅有一台閃爍卡車。
- **物理中心定位**：將閃爍卡車 `.floating-truck` **直接實體嵌套在當前配送站點的 `.station-marker` 內**！
  - **成效**：徹底解決了展開/折疊卡片時算百分比偏差導致的「開過頭」或「漂移」痛點，卡車永遠 100% 精準與當前站點重合。
  - **Q彈登場動效**：配合 CSS 實作 `Scale-in` 彈動進場與動態脈衝光暈（Pulse Glow）呼吸燈，介面極具 state-of-the-art 物流大腦美感。

### 6. 系統佈署
- 使用 `clasp push` 同步所有優化代碼（包括 `tracking.html` 全新 CSS 動畫、JS 邏輯、及 testing 參數）至 GAS Production 雲端。

---
**目前狀態**：經銷商派送查詢系統在隱私隔離、視覺動效、精準 ETA 預估與實體卡車定位上均已達到業界最頂尖水平。
**後續建議**：可將此成果與連結提供給經銷商客戶進行實地使用回饋，並視情況進一步優化特殊地址的路名提取精準度。

## [2026-05-18] 營運管理系統升級與 SOP 數位化對接

### 1. 2026 營運管理系統與 SOP 數位化建置計畫對接
- **現有程式對接評估**：
  - **車隊管理 (PART 5)**：評估司機端 (`Index.html`)「發車登記」安檢照片與里程強制填寫、加油/保養登記，以及「載重防呆」動態重量判定。
  - **驗貨與出貨 (PART 3)**：評估倉庫端 (`Warehouse.html`)「倉庫驗貨」之待裝車/已載走狀態勾選、批次裝車指派，以及同址群組防呆與產品縮圖功能。
  - **異常與退貨 (PART 4)**：對接臨時任務上傳與退回分公司的 LINE 通知回報系統。
- **技術架構重構 (去 n8n 化)**：
  - **完全移除 n8n 依賴**，重構 `2026_Logistics_Management_System_SOP.md` 規劃書。
  - 核心自動化（如到貨預警、48小時退貨未結案警示、每日巡檢推播）全面改由 **GAS 原生時間驅動觸發器 (GAS Triggers)** 接管，實現零成本、高穩定的全 GAS 雲端架構。
- **後續開發藍圖規劃**：
  - 定義後續三大開發缺口：**Inbound.html (進貨開櫃驗收)**、**倉庫盲驗防呆模式**、與**大板出貨錄影 (R2 串流儲存)**。

### 2. 開發環境 (VS Code / Cursor) 設定優化
- 針對 `Auto-approve terminal commands` (自動核准終端指令) 提供設定路徑與指導，解決 AI 代理執行命令時頻繁提示 "Run" 的操作痛點。

## [2026-05-07] 渲染優化、複製邏輯與轉址版相容性強化

### 1. 圖片生成與排版優化 (`DispatchSystem.html`)
- **寬度與佈局修正**：將「複製清單圖」的渲染寬度固定為 `600px`，並強制使用 `box-sizing: border-box` 與 `white-space: nowrap`。
    - **成效**：徹底解決了長地址或標籤導致圖片右側文字被切斷（Clipped）的問題。
- **美學風格升級**：
    - 採用深色漸層卡片底色 (`linear-gradient`) 與陰影效果。
    - 精簡標籤顯示（如 `[回]` 代替 `[回鶯歌]`），優化字級大小與行高。
    - 預覽視窗限制最大寬度為 `450px`，解決圖片過大導致的視覺壓迫感。

### 2. 互動反饋與動畫
- **點擊感強化**：為所有核心按鈕增加 `btn-pulse` 縮放動畫，確保用戶有明確的操作回饋。
- **異步狀態顯示**：實作按鈕 `loading-spinner` 狀態，點擊後自動顯示 `⌛ 處理中...` 並鎖定按鈕，直到 `html2canvas` 渲染完成，解決「感覺沒按到」的痛點。

### 3. 剪貼簿與轉址版相容性解決方案
- **克服 Iframe 限制**：針對轉址版 (`elitile.tw`) 的 `iframe` 跨網域安全限制（禁止存取剪貼簿），重構了複製流程：
    - **新視窗開啟法**：移除不穩定的「自動複製」按鈕，改為 **「📋 點我複製 (開啟新分頁)」**。
    - **原生複製支援**：在新分頁中自動提示用戶點擊 **右鍵 -> 複製圖片**，成功繞過瀏覽器對 `iframe` 的安全攔截。
- **備案機制**：保留「💾 下載圖片」功能，並在自動複製失敗時提供視覺化（金框閃爍）與文字提示引導手動複製。

### 4. 主介面穩定性修復
- **Header 斷行修復**：針對 `Index.html`、`Dashboard.html` 與 `DispatchSystem.html` 的頂部標題與 KPI 欄位強制加上 `white-space: nowrap`。
    - **成效**：防止在行動裝置或較窄螢幕下出現文字重疊或斷行現象。

### 5. 系統維護與佈署
- 修復了 `ReferenceError: triggerBtn/dataUrl is not defined` 等異步邏輯 bug。
- 使用 `clasp push` 同步 20 個核心與動畫組件至生產環境。

---
**目前狀態**：物流分享圖片的排版已達專業水準，且成功解決了困擾已久的轉址版複製權限問題。
**後續建議**：持續觀察司機端對於新版清單圖的閱讀反饋，如有需要可再調整特定欄位的顏色對比。

## [2026-04-30] 退回分公司流程與 LINE 通知系統優化

### 1. 前端 UI 介面優化
- **按鈕佈置**：移除「📸 當前篩選清單」，將「🔴 批次退回分公司」按鈕移至右上角黃金位置。
- **互動反饋**：為退回按鈕增加 `scale` 縮放特效與 `⏳ 處理中` 狀態鎖定，防止重複點擊並增加操作感。
- **佈局加寬**：將工作站 left 側任務清單寬度從 `580px` 提升至 `750px`，解決地址與客戶名稱折行問題。

### 2. 智慧排序邏輯 (Intelligent Sorting)
- **退回優先原則**：修改 `adminBatchReturnTasks_V11`，在保持日期由舊到新的前提下，強制將「退回分公司」的案件移至該日期組的最頂端。
- **交界處管理**：確保異常案件集中在日期變換的交界處，方便調度員第一時間處理。

### 3. LINE 通知系統升級
- **彩色格式化**：
    - 標題：`🌀 MM/DD 退回清單 🌀`（去掉年份更精簡）。
    - 顏色循環：同分公司不同案件依序使用 `🟧🟩🟨🟪🟦🟥⬜` 標記，辨識度極高。
    - 地址精簡：自動過濾縣市，僅保留「區 + 路/街」。
- **圖片整合**：直接在訊息中帶入訂單圖片網址，分公司可點擊即時核對。
- **穩定性調整**：評估縮網址 API 穩定性後，改回直接發送原始網址以確保訊息百分之百抵達。

### 4. 系統整合與測試
- **Webhook 對接**：完成 LINE Messaging API Webhook 設定。
- **ID 捕捉**：實作 `測試通知` 觸發機制，成功捕捉並永久存儲 `LINE_TARGET_ID`。
- **後端樣式**：優化 `applyProfessionalStyles_V11`，確保「退回分公司」在試算表中呈現正確的紫底白字。

---
**目前狀態**：退回工作流已完全閉環，具備自動排序、自動通知與視覺化反饋功能。
**後續建議**：可觀察分公司對於圖片連結點擊的使用頻率，未來若有需要可再評估更穩定的縮網址方案。

## [2026-04-30 晚間更新] OCR 辨識優化與配送方式可視化

### 1. OCR 辨識引擎升級 (OcrEngine.js V11.28)
- **漢樺 (WW) 特規支援**：
    - 針對漢樺純數字產品編碼（如 `101055`）進行優化，解除原有的字母優先過濾機制。
    - 調整項次分割邏輯，解決 OCR 誤將項次 `2` 辨識為 `12`、`3` 辨識為 `13` 導致的分割錯誤。
- **精準段落切割**：實作 `splitRegex` 偵測「項次+代號」模式，確保即使代碼為純數字也能正確區分不同品項。

### 2. 配送方式 (Shipping Method) 全面可視化
- **高亮度標籤**：
    - 在派車工作站卡片中，將「配送方式」（如：取加工、弘昇貨運）移至**地址後方**。
    - 採用 **橘色背景 + 呼吸燈動畫 (Pulse)** 效果，確保倉庫人員第一時間注意到特殊配送需求。
- **UI 精簡化**：
    - 移除冗餘的藍色括號標籤 `[配送方式]` 與紅字 `<收退貨>`，改由統一的呼吸燈標籤呈現。
    - 為「重複異常請注意‼️」標籤增加呼吸燈特效，提升預警等級。
- **多端同步顯示**：
    - **司機行程清單**：同步採用新版樣式，標籤位置與主工作站一致。
    - **LINE 派車圖片**：生成圖片的第一行現在會包含明確的配送方式標註。
    - **儀表板側邊欄**：在行程點中加入青色標籤顯示配送方式，提升管理透明度。

### 3. 系統佈署
- 使用 `clasp push` 將所有核心檔案（`OcrEngine.js`, `DispatchSystem.html`, `DispatchLogic.js`, `Dashboard.html`）同步至環境。

---
**目前狀態**：系統已具備更強大的非標單據辨識能力，且特殊任務（加工單）在調度端的可視化程度大幅提升。
