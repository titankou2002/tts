# 🚚 鈦傳速智慧物流系統 - 統一記憶規範 (V8.8)

> **⚠️ AI 助手核心記憶錨點：這是唯一的規範文件，任何修改前必須先讀這份。**

---

## 1. 派遣站 (DispatchSystem.html) UI 規範
### 🃏 任務卡片 - 嚴格兩行布局（嚴禁亂改）
- **L1**：`[複選框]` `[客戶名稱]` `[單號尾碼顏色標記]` `[(退) 紅字]` `[地址]` `[右對齊：司機/車牌標籤]`
- **顯示規則**：前台不直接顯示分公司名稱，一律以 `15529006-喜` 這類單號尾碼承載顏色與歸屬。
- **L2**：`[回鶯歌 按鈕]` `[尺寸]` `[箱數]` `[重量(放大)]` `[右對齊：待指派紅標]` `[時間]` `[日期黃色]`

### 🎨 重量顏色門檻（嚴禁更動）
| 範圍 | 顏色 | Hex |
|------|------|-----|
| < 200kg | 灰 | `#94a3b8` |
| 200~500kg | 綠 | `#22c55e` |
| 500~1000kg | 黃 | `#fbbf24` |
| > 1000kg | 紅 | `#ef4444` |

### 其他 UI 規範
- **同地址偵測**：左側邊框 → 紅色加粗
- **已指派卡片**：透明度 `0.55`
- **儀表板標題**：固定為「**司機分配載重**」
- **圖片寬度**：LINE 分享圖固定 `420px`

---

## 2. 數據邏輯規範
### ⚖️ 動態載重計算
- **公式**：`車輛總載重 = Σ(所有任務) - Σ(已完成/已送達/結案)`
- 司機卸貨後，載重條即時扣減，反映真實剩餘空間。

### 🔄 指派判定邏輯
- 只要「派遣司機」**或**「車牌」任一欄有值，即判定為「已指派」
- 狀態不是「待指派」也算已指派
- Dashboard 指派後，派遣站卡片必須立刻取消「待指派」紅標

### 📋 狀態過濾
- 跳過清單：`已完成`、`已送達`、`結案`、`取消`
- 待指派清單置頂，已指派的卡片透明變暗

---

## 3. 戰情室 (Dashboard.html) 規範
- **已完成點位**：`opacity: 0.1` + `grayscale(1)` + `brightness(0.4)`（徹底淡化）
- **隱藏按鈕**：🏁 按鈕切換顯示/隱藏已完成點位，點擊後立即重繪地圖
- **現況規模**：6,117 行 | 279KB | 132 個函式

### 📷 圖片生成比例
- LINE 分享圖寬度：`420px`，緊湊排版，確保手機一屏看全

---

## 4. 智慧派遣大數據記錄器 (V15.2/15.3)
### 排車習慣記錄表欄位（14 欄，依序貼入第一行）
```
派遣日期	單號	行政區	客戶名	重量 (kg)	箱數	尺寸規格	車牌	車次	當趟總載重	載重利用率 %	回鶯歌	分公司	操作人
```

### 雙端觸發
- **派遣工作站** → `recordDispatch_V3` → `logDispatchHabit_V8`
- **戰情室** → `adminDispatchTasks` → `logDispatchHabit_V8`（標記「戰情室指派」）
- 行政區自動辨識：`extractDistrict(addr)` 抓取「XX區」

---

## 5. Dashboard.html 模組分割計畫
### GAS 三大限制（踩雷必死）
1. **不支援 import/export**：只能用 `<?!= include('file') ?>` 切模組
2. **全域狀態共享**：`allTasks`, `map`, `selectedCar` 等需統一為 `AppState{}`
3. **函式相依順序**：切錯順序會 `undefined` 炸裂，分割前必須先畫相依圖

### 三階段分割（按順序，不准跳）
| 階段 | 目標 | 風險 |
|------|------|------|
| Phase 1 | 抽出 `OcrModal.html` | 低（最獨立） |
| Phase 2 | 抽出 `SettingsModal.html` | 中 |
| Phase 3 | 地圖與核心邏輯 | 高（等穩定再動） |

---

## 6. OCR 引擎規範 (OcrEngine.js)
- **尺寸防呆**：若數值 > 300，迴圈移除前綴字元直到 ≤ 300
- **客戶名稱 Fallback**：標頭區沒抓到 → 全文搜尋
- **地圖提示**：懸停顯示尺寸 + 箱數

---

## 7. 關鍵檔案清單
| 檔案 | 職責 |
|------|------|
| `DispatchSystem.html` | 派遣工作站 UI（V8.2 兩行格式） |
| `DispatchLogic.js` | 後端：讀取/寫入/記錄/扣重 |
| `Dashboard.html` | 戰情室：地圖、清單、OCR、設定 |
| `Code.js` | GAS 核心：adminDispatchTasks 等 |
| `APP_SPECIFICATION.md` | **唯一記憶檔（就是這個）** |

---

## 8. 運費系統設計規範 (V1.0) - 2026-04-14 共識
### 🎯 開發戰略：二階段開發
- **Phase 1**：建置 `FreightEngine` 邏輯，實現分區自動判定與「參考運費」計算。
- **Phase 2**：串接司機端 App（異常回報）與行政端實收確認。

### 💰 參考運費計費模型 (Multi-stop Focus)
針對「多點、少量 (200~500kg)」的業務特性，捨棄純重量計費，改採：
- **公式**：`參考運費 = [分區基礎點費] + [重量加成]`
- **分區基礎點費**：依地址自動判定 Zone 0~8，每站有一個起跳保護價。
- **重量加成**：500kg 以下不加價，500kg~1000kg 及以上設定階梯式加成。

### 🚚 UI 整合規範 (維持 2 行佈局)
- **原則**：嚴禁擴增為 4 行。
- **作法**：將 `[分區]` 與 `[參考運費]` 資訊擠入第 2 行。
- **預覽顯示**：在派遣確認前，UI 需即時顯示「自動判定分區」供人員預視。

### 📊 試算表欄位對標 (待新增)
- **派送清單**：自 Column AE 起新增 `配送分區` | `參考運費` | `異常類型` | `實收運費` | `運費確認備註`
- **運費管理表**：存放 Zone 0~8 的基礎點費與重量階梯參數。

---
**核准版本：V8.8 (含分公司標準化) | 最後更新：2026-04-28 18:00**
